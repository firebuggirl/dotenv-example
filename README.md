To see SECRET_MESSAGE and TOKEN (..etc,..) logged out in terminal run:

node bin/dev

Place constants/variables (UPPERCASE) in .env file
assign sensitive information, passwords, tokens, etc,.. to these constants
